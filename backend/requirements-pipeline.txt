# Data pipeline scripts only (data_consolidation.py, generate_embeddings.py).
# Run locally: pip install -r requirements-pipeline.txt
# NOT installed on Render.

-r requirements.txt
pandas==2.2.2
numpy==1.26.4
tqdm==4.66.4
colorama==0.4.6
openpyxl==3.1.4
