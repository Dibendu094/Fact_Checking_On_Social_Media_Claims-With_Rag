# API server — installed on Render.
# Uses HuggingFace Inference API for embeddings (HF_API_TOKEN env var),
# so torch and sentence-transformers are NOT needed here.
# For local dev without HF_API_TOKEN, install requirements-pipeline.txt
# which includes sentence-transformers + torch.

# --- Core ---
pydantic==2.7.4
python-dotenv==1.0.1
python-dateutil==2.9.0
numpy>=1.24.0

# --- Vector store ---
pinecone>=5.0.0

# --- API server ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0
groq>=0.11.0
