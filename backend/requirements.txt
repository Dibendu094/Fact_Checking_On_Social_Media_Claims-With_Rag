# API server — installed on Render
# Pipeline scripts (data_consolidation.py, generate_embeddings.py) use
# requirements-pipeline.txt instead, which extends this file.

# --- Core ---
pydantic==2.7.4
python-dotenv==1.0.1
python-dateutil==2.9.0

# --- Embeddings + vector store ---
sentence-transformers==3.0.1
pinecone>=5.0.0
torch>=2.2.0

# --- API server ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0
groq>=0.11.0
